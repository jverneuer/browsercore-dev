# fetch-engineer — Context Pack

> Auto-generated by scripts/memory-contextpack.ts on 2026-08-08 05:25:15
> Home scope(s): patterns, contracts

## Scope Declaration
You are the **fetch-engineer**. Your home scope is patterns, contracts.

## Essential Facts (top 4 by weight)

### patterns/platform-threading (weight: 1.00)
Platform is threaded through options, not globals: FetchClientOptions.platform → client.ts resolves net/dns from platform → dispatch.ts openTcpTransport(url, net, dns) → connectTransport({host, port, net, dns}).

### patterns/polyrepo-dep-resolution (weight: 1.00)
The orchestrator reads browsersmith's package.json, resolves each @browsercore/* dep to its git repo via repository.url, and builds a topological DAG. Work bubbles up layer by layer.

### contracts/platform-interface (weight: 1.00)
Platform interface: { network: { tcp: Net, dns: DnsResolver, udp: UdpTransport }, crypto: CryptoProvider, compression: CompressionProvider, events: EventEmitter, telemetry, time }. Defined in @browsercore/contracts.

### contracts/transport-interface (weight: 1.00)
Transport interface: { id, state, read(): Promise<Uint8Array>, write(data): Promise<void>, close(): Promise<void> }. Extends EventEmitter. States: connecting, open, closed.

