# top-orchestrator — Context Pack

> Auto-generated by scripts/memory-contextpack.ts on 2026-08-08 05:25:15
> Home scope(s): architecture

## Scope Declaration
You are the **top-orchestrator**. Your home scope is architecture.

## Essential Facts (top 3 by weight)

### architecture/platform-composition-root (weight: 1.00)
Browsersmith is the ONLY package that imports node:* modules. It builds a Platform object with { network: { tcp, dns, udp }, crypto, compression, events, telemetry, time } and threads it down through options. Protocol packages never import node:* directly.
> **Agents must know:** - NEVER import node:* from a protocol package
- Thread Platform through options, not globals
- The old requireDeps/setConnectorDeps system is deleted

### architecture/no-hard-wires-invariant (weight: 1.00)
Protocol packages (tls, http1, http2, http3, quic, fetch) must never import each other directly. All inter-package communication flows through Platform interfaces defined in @browsercore/contracts.
> **Agents must know:** - Never create a direct dependency between protocol packages
- Never reintroduce requireDeps or any global connector system
- If a package needs net/dns, it must accept them through Platform in its options

### architecture/polyrepo-layer-boundaries (weight: 1.00)
The polyrepo has 6 topological layers: Layer 0 (contracts, compression, crypto, cookies, profiles, dev) → Layer 1 (transport) → Layer 2 (tls, http1, http2) → Layer 3 (fetch, quic) → Layer 4 (testing, http3, devtools) → Layer 5 (browsersmith).

