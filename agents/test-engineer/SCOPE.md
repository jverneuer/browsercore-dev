# test-engineer — Context Pack

> Auto-generated by scripts/memory-contextpack.ts on 2026-08-08 05:25:15
> Home scope(s): operational, bugs

## Scope Declaration
You are the **test-engineer**. Your home scope is operational, bugs.

## Essential Facts (top 5 by weight)

### operational/npm-publish-flow (weight: 1.00)
CI auto-publishes on merge to main. The reusable CI workflow (browsercore-dev/.github/workflows/reusable-ci.yml) runs: npm install → typecheck → lint --deny-warnings → test --coverage → build → publish. NPM_TOKEN is in GitHub secrets.
> **Agents must know:** - Merging to main triggers auto-publish
- Coverage threshold is >=93% (statements/branches/functions/lines)
- Lint runs with --deny-warnings (zero tolerance)

### operational/ci-reusable-workflow (weight: 1.00)
Every repo uses the same reusable CI from browsercore-dev: jverneuer/browsercore-dev/.github/workflows/reusable-ci.yml@main. Node 26, deploy-pages enabled.

### operational/current-versions (weight: 1.00)
As of 2026-08-08: tls 0.4.1, transport 0.2.4, fetch 0.2.4, crypto 0.2.1, contracts 0.2.1 (local 0.2.0), quic 0.2.2 (local 0.2.1), browsersmith 0.0.4, testing 0.2.0.

### bugs/bug6-key-schedule (weight: 1.00)
Bug 6 (FIXED in tls 0.4.1): Derive-Secret(., "derived", "") in RFC 8446 §7.1 uses Transcript-Hash("") = Hash(empty_bytes) as context — NOT empty bytes. The code passed new Uint8Array(0) (0 bytes) instead of Hash(new Uint8Array(0)) (32 bytes), producing wrong HKDF-Extract salt and corrupting all downstream traffic keys.
> **Agents must know:** - This was the critical blocker preventing any real HTTPS request
- The fix is in deriveHandshakeTrafficSecrets()
- Verified end-to-end: full TLS 1.3 handshake succeeds, app data decrypts correctly

### bugs/bug2-adapter-fallback (weight: 1.00)
Bug 2 (FIXED): fetch imported requireDeps from transport as a global fallback when net/dns were not provided. This created a hard dependency chain. Fixed by threading Platform through options and throwing FetchError if net/dns missing.

