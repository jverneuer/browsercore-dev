on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-and-publish:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      node-version: '26'
    permissions:
      contents: write
      id-token: write
    secrets: inherit
